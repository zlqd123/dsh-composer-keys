# Dependencies (tests are source-eval'd; the host resolves schemastery itself)
node_modules/

# Local agent knowledge — deliberately NOT published (npm files whitelist
# excludes it too): syncs to GitHub only as the working copy.
SKILL.md

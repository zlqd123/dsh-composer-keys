# Dependencies (tests are source-eval'd; index.js resolves schemastery from
# the host at load time, so a local install here stays optional)
node_modules/

# Local agent knowledge — deliberately NOT published (npm files whitelist
# excludes it too): syncs to GitHub only as the working copy.
SKILL.md
